"use client"

import React from "react";
import MenuSection from "../MenuSection";
import { MenuData } from "../MenuData";

const SmootheMenu: React.FC = () => {
  return <MenuSection menuData={MenuData.smoothe} />;
};

export default SmootheMenu;