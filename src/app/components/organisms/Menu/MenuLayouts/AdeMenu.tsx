"use client"

import React from "react";
import MenuSection from "../MenuSection";
import { MenuData } from "../MenuData";

const AdeMenu: React.FC = () => {
  return <MenuSection menuData={MenuData.ade} />;
};

export default AdeMenu;